h2 Add a new contact
label(for='name') Name:
input(ng-mode='form.name', name='name')
p Body:
  textarea(ng-model='form.events', cols='50', rows='15')
p
  button(ng-click='submitContact()') Submit Post